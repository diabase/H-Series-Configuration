; cancel.g
; If the axes are homed and if a print is being cancelled (M25), cancel.g is called when M0 or M1 are sent.

; If a job is canceled from a pause state, we want the machine state and configuration to remain unchanged, so this file is intentionally blank.
